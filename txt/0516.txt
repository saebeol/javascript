ezenmr.step.or.kr

5월 17/24일
6월 9/16/23/30일

과목별로 기간이 정해져있다.
-------------------------------------
ㅡmap
filter
forEach

생선, 고둥어, 뉵개장
-> 파라미터, 아큐먼트, 인자

음식 -> 매개변수

food="밥"
기본파라미터

기본파라미터는 인터넷익스플로러에서는 실행이 안된다

function 함수명( ){  }
함수가 객체에 들어가면
메소드가 된다.

객체명.메소드( );
function 먹다(){ }
고양이.먹다();

조건을 두가지로 만드는 방법
2로 나눈 나머지를 활용

h1.innerHTML
h1태그의 글자노드
<h1>textNode</h1>

(i%2==0) => true or false
(i%2) => 0, 1
(i%2==1) => true or false

.innerHTML

아이디는 변수에 따로 저장할 필요없이
바로 선택이 가능하다

onclick
onmouseover
onmouseout
ondblclick 더블클릭


콜스택 영역 -> stack overflow발생
call stack

문서객체모델
배열
증감연산자, 나머지연산자
조건문, 대입연산자
함수,
자바스크립트 스타일속성
고전이벤트

//표준이벤트
button.addEventListener("click",function(){

});

button.addEventListener("click",콜백함수);

콜백함수 : 어떤일이 끝마피면 실행되는 함수

일반함수(선언함수)와 익명함수
내장함수와 외장함수
내부함수와 외부함수
콜백함수
스택영역(콜스택)
함수호출과 함수선언
- 객체를 리턴하는 함수
- 함수를 리턴하는 함수
생성자함수 : new Date()
화살표함수
함수의 매개변수가 배열인 경우
함수의 매개변수가 객체인 경우